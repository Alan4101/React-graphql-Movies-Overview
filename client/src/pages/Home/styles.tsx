const styles = {
  cardWrapper: {
    display: 'grid',
    width: '100%',
    gridTemplateColumns: 'repeat(auto-fill, minmax(180px, 1fr))',
    position: 'relative',
    gridGap: '2rem'
  }
}

export default styles
